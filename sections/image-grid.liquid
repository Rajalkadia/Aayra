{{ 'image-grid.css' | asset_url | stylesheet_tag }}
{%- style -%}
  #image-grid-{{ section.id }} {
    height: {{ section.settings.section_height_mobile }}px;
    --section-mobile-height: {{ section.settings.section_height_mobile }}px;
    background: {{ section.settings.background_color }};
  }
   #image-grid-{{ section.id }} .image-grid-wrapper .image-grid-item img,
    #image-grid-{{ section.id }} .image-grid-wrapper .image-grid-item svg{
      height: {{ section.settings.section_height_mobile }}px;;
      aspect-ratio: 375/{{section.settings.section_height_mobile}};
    }
  @media screen and (min-width: 769px) {
     #image-grid-{{ section.id }}{
      height: {{ section.settings.section_height_desktop }}px;
      --section-desktop-height: {{ section.settings.section_height_desktop }}px;
    }
    #image-grid-{{ section.id }} .image-grid-wrapper .image-grid-item img,
     #image-grid-{{ section.id }} .image-grid-wrapper .image-grid-item svg{
      height: {{ section.settings.section_height_desktop }}px;;
      aspect-ratio: 480/{{section.settings.section_height_desktop}};
    }
  }
{%- endstyle -%}
<section id="image-grid-{{ section.id }}" class="image-grid">

  <div class="image-grid-wrapper relative">
    {% for block in section.blocks %}
      <div class="image-grid-item flex {%  if forloop.index == 1 or forloop.index == 3 %} small-hide {% endif %}">
          {% if block.settings.image != blank%}
            {{ block.settings.image  | image_url: width: 2500, height:3500 | image_tag  }}
          {% else %}
              {{ 'collection-apparel-' |append: forloop.index | placeholder_svg_tag: 'w-100' }}
        {% endif %}
        </div>
    {% endfor %}
     <div class="image-grid-logo-item-wrapper">
        <div class="item-inner-wrapper">
          {% if section.settings.logo_image != blank %}
            {{ section.settings.logo_image  | image_url: width: 500, height:200 | image_tag: loading: "eager", fetchpriority: "high", decoding: 'async'  }}
          {% endif %}
           {% if section.settings.logo_text != blank %}
            <div class="header-logo-fonts" style="color: {{ section.settings.text_color }}">{{ section.settings.logo_text }}</div>
          {% endif %}
        </div>
      </div>
  </div>


</section>

{% schema %}
  {
    "name": "t:additionl.footer.image_grid.label",
    "tag": "section",
    "max_blocks": 3,
    "enabled_on": {
      "groups": ["footer"]
    },
    "settings": [
      {
        "type": "color",
        "id": "background_color",
        "label": "t:additionl.background_color",
        "default": "#000000"
      },
      {
        "type": "color",
        "id": "text_color",
        "label": "t:additionl.text_color",
        "default": "#ffffff"
      },
      {
        "type": "range",
        "id": "section_height_desktop",
        "label": "t:additionl.footer.image_grid.section_height_desktop",
        "min": 50,
        "max": 150,
        "step": 1,
        "unit": "px",
        "default": 70
      },
      {
        "type": "range",
        "id": "section_height_mobile",
        "label": "t:additionl.footer.image_grid.section_height_mobile",
        "min": 30,
        "max": 130,
        "step": 1,
        "unit": "px",
        "default": 50
      },
      {
        "type": "image_picker",
        "id": "logo_image",
        "label": "t:additionl.footer.image_grid.logo_image"
      },
       {
        "type": "text",
        "id": "logo_text",
        "label": "t:additionl.footer.image_grid.logo_text"
      },
      
    ],
    "blocks": [
      {
        "type": "Image",
        "name": "t:additionl.image",
        "limit": 3,
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "t:additionl.image"
          }
        ]
      },
    ],
    "presets": [
      {
        "name": "t:additionl.footer.image_grid.label",
      }
    ]
  }
{% endschema %}